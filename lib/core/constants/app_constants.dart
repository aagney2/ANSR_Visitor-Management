class AppConstants {
  AppConstants._();

  static const int draftPollIntervalSeconds = 5;
  static const int draftPollTimeoutSeconds = 120;
  static const int apiTimeoutSeconds = 60;

  // Visitor type options are fetched dynamically from Kelsa's visitor_type field
}
